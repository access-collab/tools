# Development


## Structure

- `src/lib`: components
- `src/routes` SvelteKit routes
- `src/api`: generated OpenApi SDK

When changing the design of the components, you can use the dev routes `src/routes/dev` resp. `http://localhost:8000/dev/...`
